{
    "name": "rouenssi/scims-backend",
    "description": "Un CMS open-source pour les scientifiques",
    "require": {
        "slim/slim": "^3.5",
        "propel/propel": "~2.0@dev"
    },
    "config": {
      "process-timeout": 0
    },
    "license": "GPL-3.0",
    "autoload": {
      "classmap": ["app/"]
    },
    "scripts": {
      "install-db": [
        "propel model:build",
        "propel sql:build",
        "propel sql:insert",
        "propel config:convert",
        "@composer dump-autoload"
      ],
      "migrate": [
        "propel diff",
        "propel migrate",
        "propel model:build",
        "@composer dump-autoload"
      ],
      "fake-data": [
        "\\SciMS\\Scripts\\FakeFixture::onMigrate"
      ],
      "test": [
        "PHP_ENV=test propel sql:build --overwrite",
        "PHP_ENV=test propel sql:insert",
        "PHP_ENV=test propel model:build",
        "PHP_ENV=test propel config:convert",
        "phpunit tests/ --bootstrap generated-conf/test/config.php"
      ],
      "dev-server": [
        "php -S localhost:3000 -t app/"
      ]
    },
    "require-dev": {
        "phpunit/phpunit": "^5.6",
        "fzaninotto/faker": "^1.6"
    }
}
